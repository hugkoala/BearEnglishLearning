# 🐻 Bear English Learning

一款專為個人英文學習設計的 Android App，透過每日情境句練習、模擬對話、YouTube 聽力測驗、每日單字表、語音對比和備忘錄複習，幫助你輕鬆提升英文能力。

**五大功能分頁**：📋 每日任務 · 💬 模擬對話 · 🎧 聽力測驗 · 📚 單字表 · 📒 備忘錄

---

## ✨ 功能特色

### 📋 每日任務 (Daily Task)
- 每天隨機抽取一個生活情境（共 30 個場景，300 句常用英文）
- 模式切換：📖 預設場景 vs 🎲 隨機生成（FilterChip 切換）
- **隨機生成模式**：20 個場景模板 × 10 句，每天自動更換
- 每個場景附中文翻譯與發音技巧
- 支援 **TTS 語音播放**（慢速 0.7x / 正常 1.0x）
- 完成任務後可直接跳轉聽力測驗

### 🎧 聽力測驗 (Listening Quiz)
- **YouTube 影片縮圖預覽**：顯示影片縮圖（Coil 載入）、標題與頻道名稱
- 點擊縮圖或「在 YouTube 中觀看」按鈕，直接跳轉 YouTube App 或瀏覽器播放
- 根據當日場景自動搜尋相關英文學習影片（預設搜尋 20 部）
- 影片瀏覽：上一部 / 下一部 / 重新載入
- 支援 **語音錄製** 與 **即時語音辨識**（優先使用裝置端辨識）
- **Word-Level Diff 對比**：使用 LCS 演算法逐字比對，以顏色標示：
  - 🟢 綠色 = 匹配正確
  - 🔴 紅色刪除線 = 漏說的單字
  - 🟠 橘色 = 多說的單字
- 自動計算準確度百分比並記錄練習歷史

### � 模擬對話 (Conversation Simulation)
- **30 組預設對話**（共 240 句），涵蓋日常生活情境
- **隨機對話生成器**：10 個場景模板 × 8 個對話欄位 × 3-4 變體 = **10,000+ 種組合**
- 模式切換：📖 預設對話 vs 🎲 隨機生成（FilterChip 切換）
- 聊天泡泡式 UI，逐句揭露，支援中英對照

### 📚 每日單字表 (Daily Vocabulary)
- **200+ 精選英文單字**，涵蓋 14 個分類：
  - 日常生活、旅遊、美食、健康、工作、情緒、教育、科技、購物、自然、社交、交通、居家、娛樂、財務、常用詞
- 每個單字包含：單字、詞性、音標、英文釋義、中文釋義、例句（中英）
- **每日自動生成 10 個單字**，使用日期種子確保當天相同
- 卡片式 UI，可展開查看例句，附分類標籤

### 📒 備忘錄 (Memo)
- 隨時記錄學習心得與筆記
- 可關聯相關練習場景
- **隔天早上 9:00 自動推播通知**提醒複習
- 複習頁面支援逐條標記「已複習」或一鍵全部完成

### ⚙️ 設定 (Settings)
- **每日任務數量**：1-10 題滑桿調整（預設 5 題）
- **難度設定**

### 🎓 首次啟動引導 (Onboarding)
5 頁引導流程，協助設定：
1. 歡迎頁面
2. TTS 語音引擎檢查與下載
3. 語音辨識引擎檢查
4. 麥克風權限授權
5. 通知權限授權（Android 13+）

---

## 🏗️ 技術架構

### 整體架構
```
MVVM + Repository Pattern + Clean Architecture
```

### 技術棧

| 類別 | 技術 | 版本 |
|------|------|------|
| 語言 | Kotlin | 2.0.0 |
| UI 框架 | Jetpack Compose + Material3 | BOM 2024.06.00 |
| 依賴注入 | Hilt | 2.51.1 |
| 本地資料庫 | Room | 2.6.1 |
| 網路請求 | Retrofit + OkHttp + Moshi | 2.11.0 / 4.12.0 / 1.15.1 |
| 導航 | Navigation Compose | 2.7.7 |
| 偏好設定 | DataStore Preferences | 1.1.1 |
| 背景任務 | WorkManager | 2.9.1 |
| 圖片載入 | Coil Compose | 2.6.0 |
| 編譯工具 | AGP / Gradle | 8.4.2 / 8.7 |
| 最低 SDK | Android 8.0 (API 26) | - |
| 目標 SDK | Android 15 (API 35) | - |

### 專案結構

```
app/src/main/java/com/bear/englishlearning/
├── BearApp.kt                          # Application（Hilt、通知頻道、WorkManager 排程）
├── MainActivity.kt                     # 主 Activity
├── data/
│   ├── local/
│   │   ├── AppDatabase.kt              # Room 資料庫（6 張表）
│   │   ├── SeedData.kt                 # 預載資料（30 場景 × 10 句、30 對話 × 8 句）
│   │   ├── SeedDatabaseCallback.kt     # 首次建立資料庫時寫入種子資料
│   │   ├── dao/                        # 5 個 DAO 介面
│   │   └── entity/                     # 6 個 Entity 類別
│   ├── preferences/
│   │   └── AppPreferences.kt           # DataStore 偏好設定
│   ├── remote/
│   │   ├── YouTubeApiService.kt        # YouTube Data API v3 介面
│   │   └── dto/                        # API 回應 DTO
│   └── repository/                     # 5 個 Repository
├── di/
│   ├── ApplicationScope.kt             # Qualifier 註解
│   ├── DatabaseModule.kt               # 資料庫 DI 模組
│   └── NetworkModule.kt                # 網路 DI 模組
├── domain/
│   ├── conversation/
│   │   └── RandomConversationGenerator.kt  # 隨機對話生成器（10 模板 × 8 欄位 × 3-4 變體）
│   ├── model/                              # Domain 模型
│   ├── scenario/
│   │   ├── DailyScenarioGenerator.kt       # 每日場景生成器（20 模板 × 10 句）
│   │   ├── GeneratedScenario.kt            # 生成場景資料模型
│   │   └── GeneratedSentence.kt            # 生成句子資料模型
│   ├── speech/
│   │   ├── TextNormalizer.kt               # 文字正規化（縮寫展開、標點移除）
│   │   └── WordDiffEngine.kt               # LCS 逐字比對演算法
│   └── vocabulary/
│       ├── DailyVocabularyGenerator.kt     # 每日單字生成器（200+ 單字、日期種子）
│       └── VocabularyWord.kt               # 單字資料模型
├── ui/
│   ├── MainViewModel.kt                    # Onboarding 狀態管理
│   ├── components/
│   │   ├── BearIcon.kt                     # 🐻 熊頭圖示元件
│   │   └── SpeechDiffDisplay.kt            # 語音比對結果顯示元件
│   ├── navigation/
│   │   └── AppNavigation.kt                # 導航圖（5 個底部分頁 + 子頁面）
│   ├── screens/
│   │   ├── conversation/                   # 模擬對話頁面 + ViewModel
│   │   ├── dailytask/                      # 每日任務頁面 + ViewModel
│   │   ├── listening/                      # 聽力測驗頁面 + ViewModel
│   │   ├── memo/                           # 備忘錄頁面 + ViewModel
│   │   ├── onboarding/                     # 引導頁面（5 頁 HorizontalPager）
│   │   ├── review/                         # 複習頁面 + ViewModel
│   │   ├── settings/                       # 設定頁面 + ViewModel
│   │   └── vocabulary/                     # 每日單字表頁面 + ViewModel
│   └── theme/                          # Material3 主題（Color、Theme、Type）
└── worker/
    └── DailyReviewReminderWorker.kt    # 每日複習提醒 Worker

app/src/test/java/com/bear/englishlearning/domain/speech/
├── TextNormalizerTest.kt               # 文字正規化單元測試
└── WordDiffEngineTest.kt               # 逐字比對單元測試
```

---

## 📊 資料庫設計

共 8 張表：

| 表名 | 用途 |
|------|------|
| `scenarios` | 30 個英文學習場景 |
| `sentences` | 300 句英文例句（每場景 10 句） |
| `daily_tasks` | 每日任務記錄 |
| `practice_history` | 語音練習歷史與準確度 |
| `memos` | 學習備忘錄 |
| `cached_videos` | YouTube 影片快取（7 天 TTL） |
| `conversations` | 30 組模擬對話 |
| `conversation_lines` | 240 句對話內容（每組 8 句） |

### 預載場景清單

| # | 場景 | # | 場景 |
|---|------|---|------|
| 1 | 餐廳點餐 | 16 | 談論天氣 |
| 2 | 商店購物 | 17 | 電影院 |
| 3 | 問路 | 18 | 求職面試 |
| 4 | 機場 | 19 | 藥局 |
| 5 | 飯店入住 | 20 | 大眾運輸 |
| 6 | 看醫生 | 21 | 派對社交 |
| 7 | 超市 | 22 | 餐廳訂位 |
| 8 | 搭計程車 | 23 | 退換貨 |
| 9 | 銀行 | 24 | 美髮沙龍 |
| 10 | 打電話 | 25 | 聊興趣 |
| 11 | 咖啡廳 | 26 | 加油站 |
| 12 | 郵局 | 27 | 緊急求助 |
| 13 | 健身房 | 28 | 海灘度假 |
| 14 | 認識新朋友 | 29 | 拜訪朋友 |
| 15 | 圖書館 | 30 | 辦公室日常 |

---

## 🔑 API 設定

本專案使用 **YouTube Data API v3** 搜尋相關英文學習影片。

API Key 透過 `app/build.gradle.kts` 中的 `buildConfigField` 設定，編譯後可透過 `BuildConfig.YOUTUBE_API_KEY` 取得。

### 快取策略
- 搜尋結果快取 **7 天**
- 優先讀取本地快取，網路錯誤時 fallback 至快取
- 節省 YouTube API 每日 10,000 單位配額

---

## 🗣️ 語音比對演算法

### TextNormalizer
1. 轉換為小寫
2. 展開 **50+ 英文縮寫**（e.g. `I'd` → `I would`, `won't` → `will not`）
3. 移除標點符號
4. 壓縮多餘空白

### WordDiffEngine (LCS)
使用 **Longest Common Subsequence** 動態規劃演算法：
- 時間複雜度：$O(m \times n)$
- 空間複雜度：$O(m \times n)$
- 回溯 DP 表格產生 `MATCH` / `MISSING` / `EXTRA` 標記
- 準確度 = 匹配字數 ÷ 目標句總字數

---

## 🚀 建置與執行

### 前置需求
- JDK 17
- Android SDK (API 35)
- Android Build Tools 35.0.0

### 建置步驟

```bash
# 設定 Android SDK 路徑
echo "sdk.dir=/path/to/your/Android/Sdk" > local.properties

# 編譯 Debug APK
./gradlew assembleDebug

# 執行單元測試
./gradlew test

# APK 輸出路徑
# app/build/outputs/apk/debug/app-debug.apk
```

### 安裝到裝置

```bash
adb install app/build/outputs/apk/debug/app-debug.apk
```

---

## 🧪 測試

目前包含以下單元測試：

- **TextNormalizerTest**：驗證文字正規化（小寫轉換、標點移除、縮寫展開）
- **WordDiffEngineTest**：驗證 LCS 比對（完全匹配、大小寫、缺字/多字、準確度計算）

```bash
./gradlew test
```

---

## 📱 截圖

> 待補充

---

## 📄 授權

本專案僅供個人學習使用。
